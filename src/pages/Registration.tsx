import { RegistrationForm } from '@features/authorization/components/RegistrationForm';

interface IRegistrationProps {

}

export function Registration(props: IRegistrationProps) {

    return (
        <RegistrationForm />
    );
};